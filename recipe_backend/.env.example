# Required
POSTGRES_URL=postgresql://appuser:dbuser123@localhost:5000/myapp

# Recommended (set in production)
JWT_SECRET_KEY=change-me
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRES_MINUTES=60

# CORS (comma-separated, or "*" for any)
CORS_ALLOW_ORIGINS=*
